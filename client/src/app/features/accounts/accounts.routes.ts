import {Routes} from '@angular/router';
import {Login} from '@/features/accounts/components/login/login';

export const accountsRoutes: Routes = [

  {
    path: 'login',
    component: Login,
  }

]
